import {
  Loader2,
  Mail,
  Lock,
  User,
  type LucideIcon,
} from "lucide-react"

export type Icon = LucideIcon

export const Icons = {
  spinner: Loader2,
  mail: Mail,
  lock: Lock,
  user: User
}
